import { RolenameformatPipe } from './rolenameformat.pipe';

describe('RolenameformatPipe', () => {
  it('create an instance', () => {
    const pipe = new RolenameformatPipe();
    expect(pipe).toBeTruthy();
  });
});
